# Server Configuration
PORT=8080

# Database Configuration
# Use 'postgres' as hostname (service name in docker-compose)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=dyvaniest123
DB_NAME=sibvet_lampung

# JWT Configuration
JWT_SECRET=top_secret_key12345
JWT_EXPIRED_HOURS=24

# Email (SMTP) Configuration for Notification
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=divanypangestika@gmail.com
SMTP_PASSWORD=ctfz sgyg vnoz dloc
SMTP_FROM=divanypangestika@gmail.com
SMTP_TIMEOUT_SECONDS=10
SMTP_MAX_RETRIES=3

# Optional: one-time initial superadmin bootstrap
BOOTSTRAP_SUPERADMIN_EMAIL=superadmin@example.com
BOOTSTRAP_SUPERADMIN_PASSWORD=$superadmin123$
BOOTSTRAP_SUPERADMIN_FULLNAME=Initial Superadmin
BOOTSTRAP_SUPERADMIN_PHONE=080000000000
BOOTSTRAP_SUPERADMIN_POSITION=
BOOTSTRAP_SUPERADMIN_UNIT_LAB=
BOOTSTRAP_SUPERADMIN_EMPLOYEE_NO=

# Frontend login URL (used in verification approved email)
APP_LOGIN_URL=http://localhost:3000/login
